export const dynamic = 'force-dynamic'
export const dynamicParams = true
export const revalidate = 0

// This configuration will be used by all routes that need dynamic rendering
// Routes that use cookies or need server-side data should import this config 